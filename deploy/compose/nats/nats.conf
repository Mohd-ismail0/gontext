# NATS JetStream config for starter Compose.
# User/password auth is applied via nats-server CLI flags in docker-compose.starter.yaml.

jetstream {
  store_dir: /data
  max_memory_store: 256MB
  max_file_store: 2GB
}

http: 8222
