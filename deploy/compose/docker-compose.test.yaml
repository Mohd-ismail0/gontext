# Dex OIDC test IdP overlay for integration CI.
# Merges with starter stack; point OIDC_* env vars at dex in .env.starter for CI.
#
# Starter's `data` network is internal. Containers attached to an internal
# network get Docker DNS SERVFAIL ("server misbehaving") for names that are
# only on the public gateway network. CI therefore:
#   - turns off data.internal so serve can resolve dex
#   - attaches dex to both data and gateway (JWKS + published :5556)

networks:
  data:
    internal: false

services:
  dex:
    image: ghcr.io/dexidp/dex:v2.41.1
    restart: unless-stopped
    command: ["dex", "serve", "/etc/dex/config.yaml"]
    volumes:
      - ./dex/config.yaml:/etc/dex/config.yaml:ro
    networks:
      - data
      - gateway
    ports:
      - "5556:5556"

  serve:
    environment:
      OIDC_ISSUER: http://dex:5556/dex
      OIDC_AUDIENCE: context-fabric-starter
      OIDC_CLIENT_ID: context-fabric-starter
      OIDC_CLIENT_SECRET: integration-dex-secret
      OIDC_DISCOVERY_URL: http://dex:5556/dex/.well-known/openid-configuration
      OIDC_JWKS_URL: http://dex:5556/dex/keys
    ports:
      - "8080:8080"
    depends_on:
      dex:
        condition: service_started
